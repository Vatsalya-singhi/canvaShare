import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.suas.mobilesystem',
  appName: 'mobsys',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
